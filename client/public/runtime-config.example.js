// Runtime configuration for the Ticketing System frontend.
// Duplicate this file as `runtime-config.js` and adjust the `apiUrl`
// if you need to point the client at a hosted API after the build step.
// During local development you can leave the value empty.
window.__APP_CONFIG__ = window.__APP_CONFIG__ || {}
window.__APP_CONFIG__.apiUrl = window.__APP_CONFIG__.apiUrl || ''
